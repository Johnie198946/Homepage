- [x] 后端服务可在本地读取配置并成功导入应用入口
- [x] 管理员登录、登出、JWT 校验与黑名单机制符合 spec
- [x] 视频管理接口支持 CRUD、排序、状态过滤与上传校验
- [x] Collection 与 Photo 接口支持 CRUD、筛选、分页、批量操作与双语字段
- [x] About 与 AI 应用入口接口支持前台读取与后台管理
- [x] 图片访问支持 Referer 校验、3600 秒签名 URL 与版权信息返回
- [x] 分析事件上报接口满足异步返回、限流与错误兜底要求
- [x] 页面 PV、热门作品、按钮点击统计接口可用且带 5 分钟缓存
- [x] `analytics_events` 使用按自然月 Range Partitioning
- [x] 前端 Home、Gallery、About、Apps 已改为 API 驱动且视觉无明显变化
- [x] Admin 已接入真实鉴权与持久化 CRUD，且不破坏现有设计风格
- [x] 本地至少完成一次前端构建检查与后端启动/导入验证
- [x] Admin 登录后可看到明确的 `About` 编辑入口，不依赖隐含交互或其他管理分区间接进入
- [x] Admin 的 `About` 编辑界面包含双语个人介绍、双语地点信息、联系方式和独立保存按钮，且视觉风格与现有后台一致
- [x] Admin 保存 About 内容时会调用独立 `/api/about` 更新流程，提供成功/失败反馈，并在刷新后正确回显后端数据
- [x] 当 `/api/about` 返回空内容或缺少字段时，Admin 仍展示可编辑的空表单，不出现空白或崩溃
- [x] 首页公开视频接口最多返回 3 条已启用视频，并按排序权重升序排列
- [x] 首页在存在 2 至 3 条启用视频时会自动轮播，在仅有 1 条视频时保持单视频展示
- [x] 首页视频轮播不破坏现有首屏视觉、文案布局与悬浮导航交互
- [x] 后台视频管理区明确提示首页会按排序轮播前 3 条启用视频
- [x] 视频上传校验至少支持 MP4、M4V、MOV、WebM 等主流格式
- [x] 后台视频上传控件与说明文案准确体现当前支持的主流视频格式
- [x] 新增支持的视频格式可通过现有上传链路入库与存储，不支持格式仍会被正确拒绝
- [x] 已定位 MOV 首页不播放的根因，不再停留在“上传成功但前台静默失败”的不确定状态
- [x] 首页对 MOV / M4V 等兼容性不稳定视频具备兼容分发或明确降级策略
- [x] 后台对兼容性不稳定的视频格式提供明确提示，避免管理员误判首页可播性
- [x] 问题视频《聚沙成塔...》经过回归验证后可在首页播放，或能得到明确失败提示
- [ ] About 页 `Business Collaboration` 表单已接入真实后端提交流程，不再只是本地成功提示
- [ ] 商务合作留言提交后会入库，并能记录站点拥有者通知邮件状态与访客确认邮件状态
- [ ] 站点拥有者能通过邮箱收到新留言通知，访客能收到确认邮件副本
- [ ] Admin 中存在明确的商务合作留言管理入口，并支持查看详情与更新处理状态
- [ ] 当邮件发送失败时，前台反馈、后台状态和验收结果都能明确体现降级状态
