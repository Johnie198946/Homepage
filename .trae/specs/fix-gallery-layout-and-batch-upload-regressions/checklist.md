- [x] 主页 Gallery 在首张横图、后续竖图的混排场景下不会再留下可容纳一张竖图的异常空位
- [x] 主页 Gallery 的默认态与 `View All / Show Less` 展开态都使用一致的补位规则
- [x] 后台 Gallery 的 batch upload 在选择合集后会自动联动对应上下文或筛选结果
- [x] 后台 batch upload 在切换合集后不会残留旧合集的联动状态
- [x] 后台 batch upload 中对单张图片修改的 EN/CN 标题在上传后会正确保存
- [x] 未填写单张标题时仍可按既有回退规则使用默认值，但不会覆盖已编辑字段
- [x] 相关前后端页面、请求组包与接口保存逻辑通过基础验证，且不破坏现有 Gallery 设计与上传流程
